connect localhost/9160;
create keyspace Twissandra;
create column family Entity with comparator = UTF8Type 
and key_validation_class=UTF8Type 
and default_validation_class = UTF8Type 
and column_metadata = [{column_name: type, validation_class: UTF8Type, index_type: KEYS}];